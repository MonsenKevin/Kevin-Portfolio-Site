# Build output — generated by build.js, not committed directly
# (the Action copies specific files to the right locations before committing)
dist/

# Node dependencies
node_modules/

# macOS
.DS_Store